# P300 BCI Pipeline — Requirements
# Install with: pip install -r requirements.txt

# Core scientific stack
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
h5py>=3.8

# Machine learning
scikit-learn>=1.3
imbalanced-learn>=0.11    # SMOTE and class-balanced samplers

# EEG / BCI
mne>=1.5                  # Bandpass, epoch utilities
pyriemann>=0.5            # xDAWN spatial filter + Riemannian geometry

# Deep learning (EEGNet)
torch>=2.0
torchvision>=0.15

# Optional / dev
seaborn>=0.12             # Enhanced confusion matrix plots
tqdm>=4.65                # Progress bars
